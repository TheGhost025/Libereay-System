@model ApplicationUser

<div class="container mt-4 fade-in">
    <h2 class="mb-4 fade-in-title">Your Profile</h2>

    <div class="card shadow-sm profile-card fade-in-card">
        <div class="card-body fade-in-content">
            <table class="table table-borderless fade-in-table">
                <tr>
                    <th class="text-secondary">First Name</th>
                    <td>@Model.FirstName</td>
                </tr>
                <tr>
                    <th class="text-secondary">Last Name</th>
                    <td>@Model.LastName</td>
                </tr>
                <tr>
                    <th class="text-secondary">Email</th>
                    <td>@Model.Email</td>
                </tr>
                <tr>
                    <th class="text-secondary">Role</th>
                    <td>@Model.Role</td>
                </tr>
            </table>
        </div>
        <div class="card-footer text-right">
            <a class="btn btn-primary btn-animated" asp-action="EditProfile" asp-controller="User">Edit Profile</a>
        </div>
    </div>
</div>


